# 服务端地址（ws:// 或跨公网 wss://），末尾不要带斜杠，端口与服务端一致
server: "ws://127.0.0.1:39689"
# 与服务端 agent_token 一致
token: "change-me-agent-token"
# 上报间隔（秒）
interval: 5
# 监控的网卡名，留空则自动选择第一个可用非回环网卡
iface: ""
# UUID 持久化文件路径，留空使用用户配置目录；重启后保持一致以累计流量
uuid_file: ""
